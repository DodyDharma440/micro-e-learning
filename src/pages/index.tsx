import { Button } from "@geist-ui/core";
import React from "react";

const Home = () => {
  return (
    <div>
      <Button auto type="secondary">
        Geist UI Button
      </Button>
    </div>
  );
};

export default Home;

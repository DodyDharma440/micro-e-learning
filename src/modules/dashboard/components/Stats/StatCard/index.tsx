import React from "react";

import { Card } from "@nextui-org/react";

const StatCard = () => {
  return (
    <Card isBlurred className="w-full p-4">
      StatCard
    </Card>
  );
};

export default StatCard;
